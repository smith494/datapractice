{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["ecc-universal"],
  "command": {
    "goodnight": {
      "description": "Stage all, commit, and push to origin",
      "template": "{file:.opencode/commands/goodnight.md}"
    }
  }
}
